function newInstance(left, right) {
  console.log('Object.getPrototypeOf()', Object.getPrototypeOf(left))
  const proto = Object.getPrototypeOf(left)
  if(left !== 'Object') {

  }
}

console.log('aaa', String)